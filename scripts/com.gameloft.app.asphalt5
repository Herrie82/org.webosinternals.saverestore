#!/bin/sh

APPID=`basename $0`
APPNAME="Asphalt5"

source `dirname $0`/srf.app.info

APPDIR=/media/cryptofs/apps/usr/palm/applications/$APPID/Asphalt5
FILES="data.sav"

source `dirname $0`/srf.app.files

exit 0
