#!/bin/sh

source `dirname $0`/srf.defaults

APPID=`basename $0`
APPNAME="Asphalt5"
APPDIR=$USERAPPDIR/$APPID/Asphalt5

source `dirname $0`/srf.app.info

FILES="data.sav"

source `dirname $0`/srf.app.files

exit 0
