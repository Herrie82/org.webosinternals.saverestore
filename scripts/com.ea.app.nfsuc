#!/bin/sh

source `dirname $0`/srf.defaults

APPID=`basename $0`
APPNAME="NFSU"
APPDIR=$USERAPPDIR/$APPID/res_nfsuc

source `dirname $0`/srf.app.info

FILES="gamesett gamedata"

source `dirname $0`/srf.app.files

exit 0
